# Building
```
$ git clone --recursive git@github.com:PopeRigby/org.slgobinath.SafeEyes.git
$ cd org.slgobinath.SafeEyes
$ mkdir build
$ flatpak-builder --user --install build/ org.slgobinath.SafeEyes.yml --force-clean
```

# Running
```
$ flatpak run org.slgobinath.SafeEyes
```
